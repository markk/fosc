/* ------------------------------------------------------------------------------------------------------------

TITLE:: FoscSchemeSymbol


SUMMARY:: Returns a FoscSchemeSymbol.


DESCRIPTION:: Scheme symbol


USAGE::

'''
code::
a = FoscSchemeSymbol(\arrow);
a.format;
'''
------------------------------------------------------------------------------------------------------------ */
FoscSchemeSymbol : FoscScheme {
    *new { |symbol|
        ^super.new((symbol ? \cross).asString).quoting_("'");
    }
    ///////////////////////////////////////////////////////////////////////////////////////////////////////////
    // PUBLIC INSTANCE METHODS
    ///////////////////////////////////////////////////////////////////////////////////////////////////////////
    /* --------------------------------------------------------------------------------------------------------
    • symbol

    Gets symbol string.

    '''
    code::
    a = FoscSchemeSymbol(\arrow);
    a.symbol;
    '''
    -------------------------------------------------------------------------------------------------------- */
    symbol {
        ^value;
    }
}
